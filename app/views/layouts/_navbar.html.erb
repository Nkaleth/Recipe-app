<% if current_user %>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <img src="/assets/recipe-svgrepo-com.svg" alt="Logo" width="60" height="60" class="d-inline-block align-text-top">
      <a class="navbar-brand d-flex align-items-center ms-2" href="#">Recipe-App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Foods", user_foods_path(current_user), class: active?(user_foods_path(current_user)) %>
          </li>
          <li class="nav-item">
          <%= link_to "Recipes", recipes_path(current_user), class: active?(recipes_path(current_user)) %>
          </li>
          <li class="nav-item">
            <%= link_to "Public Recipes", public_recipes_path, class: active?(public_recipes_path) %>
          </li>
          <li class="nav-item">
            <%= link_to "Shopping List", general_shopping_list_path, class: active?(general_shopping_list_path) %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<% end %>
